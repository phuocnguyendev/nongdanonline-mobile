class Animal {
    constructor(name, stage, weight, carePackage) {
        this.name = name;
        this.stage = stage;
        this.weight = weight;
        this.carePackage = carePackage;
    }
}

export default Animal;
